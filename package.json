{
	"name": "xisto-landing-page",
	"private": true,
	"scripts": {
		"build": "mkdir -p dist && cp index.html dist/index.html && cp public/favicon.svg dist/favicon.svg && mkdir -p dist/fonts && cp public/fonts/* dist/fonts/",
		"deploy": "wrangler deploy",
		"dev": "npx serve dist"
	},
	"devDependencies": {
		"wrangler": "4.56.0"
	},
	"engines": {
		"node": ">=22"
	},
	"type": "module"
}
